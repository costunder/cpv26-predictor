# v5 오류 수정 검증 — 2026-08-31

이 기록은 재현된 결함과 회귀 검사 결과다. 예측 정확도 향상이나 모든 가능한 오류의
부재를 주장하지 않는다. 기존 DB·원천·그래프·checkpoint는 보존했다.

## 수정한 경로

| 오류 | 수정 및 회귀 검사 |
|---|---|
| 최근 연도 집계 정답을 보고서에 0건으로 표시 | v5 날짜·연도·전체 건수를 실제 학습 배열에서 집계. v2/v3/v4 보고서는 배열을 읽어 보정하며 기존 파일을 변경하지 않음 |
| 현대 원천의 더블헤더 번호 누락 | adapter v2에서 경기 ID·날짜·팀·연도를 검사하고 1/2차전 번호를 새 수정 행에 저장 |
| 수정된 연간 파일에서 삭제/미확정 처리된 경기·타석이 옛 버전에서 부활 | 같은 배포자·연도의 최신 알려진 전체 스냅샷을 먼저 선택. store·snapshot·두 baseline·graph에 같은 정책 적용 |
| 빈/불완전 박스스코어가 유효 PA 통계까지 가림 | 실제 제공된 필드·학습 항목에만 우선순위 적용. 제공되지 않는 항목의 PA 이력·정답은 유지 |
| 일부 PA를 완전한 공식 집계로 승격 | 공통 관측 수치가 일치할 때만 보완. H=2와 부분 PA의 TB=1 같은 모순을 만들지 않음 |
| 최신 수정본 만료 뒤 과거 정답·입력이 다시 선택됨 | 최신 알려진 수정본을 선택한 뒤 유효기간을 적용. 라벨과 rolling history 모두 회귀 검사 |
| 취소·점수 누락 경기의 이전 최종 결과가 다시 선택됨 | 취소·불완전 수정 행도 revision 선택에 포함하고 선택 후 상태·점수를 검사 |
| 동일 확인 선수의 LiveHit와 집계 정답이 서로 모순됨 | 유효한 공식 집계의 H/PA 또는 확인된 최소 PA를 우선. 집계가 제공하지 못하면 관측 PA 사용 |
| 범용 DirectPlayerGameHead의 극단 logit NLL/기울기 소실 | FP32 이상 log-sigmoid/log-softmax 계산. 파라미터·checkpoint 구조 유지 |
| object/혼합형 정답 label 조회 실패 | Python·NumPy scalar를 원래 label 타입으로 매핑 |
| 매우 큰 유한 표본 가중치에서 LL/Brier/ECE overflow | 최댓값으로 먼저 스케일한 뒤 정규화. 극대·극소 가중치의 배율 불변성 검사 |
| 보고서 경로로 DB·원천·그래프 입력을 덮어쓸 수 있음 | 현대 importer 보고서는 DB/원천 디렉터리, graph 감사 보고서는 입력 graph 디렉터리 안으로 쓰지 못하도록 차단 |
| 여러 스레드가 열린 부모 프로세스에서 DataLoader 워커를 fork | 워커가 있을 때 해당 loader만 `spawn` 사용. 워커 0은 단일 프로세스를 유지하고 전역 시작 방식은 변경하지 않음 |

원천 스냅샷 선택 기준은 `knowledge_at`이다. 삭제된 첫 경기 때문에 원천의 합성
`available_at`이 뒤로 이동해도 이전 스냅샷을 되살리지 않는다. 과거 knowledge 시점에는
당시 스냅샷을 다시 조회할 수 있고, 원문과 옛 물리 행은 남는다. 원천의 실제 발표 시각을
복원한 것은 아니므로 여전히 회고적 벤치마크다.

겹침·철회·취소 경계는 격리된 테스트 DB에서 재현했다. 현재 고정 원천의 역사
박스스코어와 현대 PA 기간은 겹치지 않으므로 해당 경계 오류가 현재 모든 학습 결과에
발생했다고 주장하지 않는다. 범용 DirectPlayerGameHead 수정도 현재 KBO RelGNN의
별도 joint-loss 경로에서 같은 수치 오류가 발생했다는 뜻이 아니다.

## 실제 DB 보존 검사

검증은 기존 전체 DB의 별도 복사본에서 실행했다.

- 기존 애플리케이션 테이블 37개를 `EXCEPT ALL`로 비교: 이전 행 삭제·변경 0건.
- 현재 선택된 경기 15,815개, 팀-경기 31,630개, 완료 PA 206,583개 유지.
- 과거 선수 박스스코어 원문 447,299행 유지.
- 더블헤더 66경기 보완: 2023년 24개, 2024년 22개, 2025년 20개.
- 같은 importer를 다시 실행한 결과 새로 삽입된 행 0개.
- schema v5의 38-table schema·참조 검사 통과.

## 전 기간 그래프 대조

2001-04-05~2026-07-26의 **3,968일**을 새 경로로 생성했다.
v5 fingerprint는 `c1776f500c93a19ec83c82d9f23b60ef4c52711e7d8c1029c4b290e5806dbedc`다.

- NPZ 3,968개 SHA-256, 배열 238,080개, 과거 시점 관계 7,748,441개 검사 통과.
- 최근 연도의 비정상적인 빈 통계 블록 0일.
- 일별·연도별·전체 사용량과 실제 학습 배열 사이 건수 불일치 0건.
- 기존 v4와 query ID 기준 전체 정답 대조: 누락·추가·중복·값 차이 0건.
- 동일 순서의 모든 입력/정답 배열과 노드 ID도 정확히 같았다.

즉 현재의 겹침/철회 없는 고정 데이터에서는 이번 v5 변경이 정상 데이터의 입력과
정답을 바꾸지 않았다. 경계 처리와 보고서를 바로잡은 것이며, 이 대조로 모델 정확도가
향상됐다고 결론 내릴 수 없다.

| 정답 종류 | 전체 질의 수 |
|---|---:|
| 경기 | 15,815 |
| LiveHit | 360,740 |
| 개별 PA | 206,579 |
| 타격 집계 | 359,002 |
| 투구 집계 | 137,456 |

전체 타격 집계의 관측 결과는 1,227,501개, 투구 집계의 관측 필드는 1,272,492개다.
아래 최근 연도의 집계 정답은 v4에서 이미 배열에 있었지만 보고서에는 0으로 잘못
표시되었다. v5는 실제 건수를 보고하고 archive-only 0건은 별도 항목에 남긴다.

| split | 타격 질의 | 타격 관측 결과 | 투구 질의 | 투구 관측 필드 |
|---|---:|---:|---:|---:|
| 2025 validation | 16,457 | 55,994 | 7,022 | 42,132 |
| 2026 부분 시즌 test | 10,717 | 37,102 | 4,593 | 27,558 |

로컬 상세 산출물은 `var/validation/all_errors_20260831/`의 `modern-import.json`,
`modern-import-retry.json`, `graph/manifest.json`, `audit.json`, `comparison.json`이다.
원천 파일과 전체 생성 산출물은 Git에 올리지 않는다.

## 코드 검사

워커 보완까지 포함한 Windows/Python 3.12.13 로컬 전체 테스트는 **531 passed, 45 skipped**다.
PyTorch·선택적 CatBoost 미설치와 Windows 권한 재현 불가 항목이 skip 사유이며,
skip을 성공한 신경망 검사로 계산하지 않았다. Ruff와 56개 소스 파일 Mypy 검사는 통과했다.

첫 수정 커밋 `c6b2ec3`의 [Conda/PyTorch CI](https://github.com/costunder/cpv26-predictor/actions/runs/33398904934)는
612 passed, 1 skipped와 wheel/의존성 검사 성공을 기록했다. CPU PyTorch 환경이므로
CUDA 실장비 항목은 제외된다. 그 로그의 두 경고는
멀티스레드 프로세스에서의 `fork`였으므로, 추가로 loader 시작 방식을 수정했다.
실제 교착이 발생했다는 주장은 아니다.

[Python 3.12 설명](https://docs.python.org/3.12/library/multiprocessing.html#contexts-and-start-methods)과
[PyTorch multiprocessing 지침](https://docs.pytorch.org/docs/stable/notes/multiprocessing.html)은
스레드·가속기 상태를 상속하는 fork의 위험과 다른 시작 방식 사용을 설명한다.
추가 변경은 워커 시작 방식뿐이며, 위 실데이터 그래프의 배열·정답·fingerprint를 바꾸지 않는다.

신경망 테스트에는 FP32/FP16/BF16 극단 logit의 손실·유한 기울기, 실제 optimizer
갱신, 기존 checkpoint 로딩, RelGNN 학습·재개·평가와 집계 gradient 경로를 포함한다.
GPU 전체 학습·실행시간·정확도 재평가는 이번 오류 수정 검사의 범위가 아니다.

## 적용

기존 환경은 [README 4-1](../README.md#4-1-기존-v3v4에서-v5로-갱신)의
캐시 재적재 → 새 v5 그래프 → 전 기간 감사 순서로 갱신한다. 환경 재설치나 원천
재다운로드는 필요 없다. 이전 checkpoint를 새 fingerprint의 그래프에 resume하지 않는다.
